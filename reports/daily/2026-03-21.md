# Nightly Status Report - 2026-03-21

## Project Health Score: 6/10
*The project health has decreased due to additional test failures identified in the editor package and observed performance regressions in store operations. Core engine functionality remains stable but rendering tests are fragile.*

## Summary
- **Tests Passing:** 171
- **Tests Failing:** 7
- **Open Issues:** 7
- **Commits Today:** 0
- **Files Changed:** 2 (this report, baseline_metrics.json)

## Key Accomplishments
- Maintained stability in the core engine and editor packages.
- Benchmarks show consistent performance for interpolation (Number, Vector3).
- Project documentation is comprehensive and up-to-date across 30+ markdown files.

## Blockers Found
- **CharacterRenderer Failures (Engine):** Three unit tests in `packages/engine/src/scene/renderers/CharacterRenderer.test.tsx` are failing with `TypeError: Cannot read properties of undefined (reading 'render')`.
- **Viewport Failures (Editor):** Four unit tests in `packages/editor/src/viewport/Viewport.test.tsx` are failing due to missing exports in `@react-three/drei` mocks (e.g., `ContactShadows`).
- **Performance Regression:** `Store Update Actor (1k ops)` has significantly regressed to ~5.2s, compared to previous baselines of ~1.9ms per op (total ~1.9s for 1k ops if consistent with March 19 memory).
- **Node Modules Initial State:** Initial environment lacked `turbo` and other dependencies, requiring `pnpm install`.

## Recommendations for Tomorrow
- **Fix CharacterRenderer Tests:** Investigate and fix the `render` property error in `CharacterRenderer.test.tsx`. Check `React.memo` and `React.forwardRef` usage.
- **Fix Viewport Mocks:** Update `Viewport.test.tsx` to correctly mock `@react-three/drei` components like `ContactShadows` and `Environment`.
- **Optimize Store Performance:** Investigate the regression in `Store Update Actor` performance. Check for unnecessary re-renders or state subscription overhead.
- **Audit Batch Progress:** Re-sync `PROGRESS.md` with the actual state of `BACKLOG.md`.
- **Cleanup Blockchain Artifacts:** Adhere to `JULES_GUIDE.md` and `PROGRESS.md` notes by deleting `packages/contracts/` and `docs/SMART_CONTRACTS.md`.
