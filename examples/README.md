# Example Scene: Cowboy Story

See `cowboy_story.json` for the full scene data.
These example JSON files demonstrate the Fredess scene format.
Use them as reference when creating scenes manually or testing the JSON import pipeline.
